{
  "name": "zilic-consult",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate:seo": "node scripts/validate-seo.mjs",
    "check": "npm run build && npm run validate:seo",
    "bundle:motion": "esbuild src/motion.js --bundle --minify --format=esm --target=es2020 --outfile=assets/motion.js"
  },
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "motion": "^13.4.0"
  }
}
